export interface Metadata {
  counts: {
    servers: number,
    raids: number,
    dungeons: number,
    grandmasters: number,
    trials: number,
    ironBanner: number
    competitiveCrucible: number
  }
}
